// if(<condition>)

/**
 * Comparision Operators: 
 * <, >, <=, >=, ==, !=, ===, !==
 * 
 * '==' checks only the value and doesn't check the data type on both sides
 * '===' checks the value as well as data type 
 */

const temp = 40;
if (temp > 50){
    // console.log("Temp>50");
}
else{
    // console.log("Temp<50");
}

// if (temp<50) console.log("temp<50");